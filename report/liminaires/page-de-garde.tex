% Page de garde.
%
% TROIS EXIGENCES GOUVERNENT CETTE PAGE, et elles se lisent dans ce qui suit.
%
% 1. QUATRE CORPS DE CARACTÈRE AU MOINS. Le document annoncé, le titre, les intitulés et les
%    valeurs ne se lisent pas au même rang, et la page doit le montrer avant d'être lue.
%    Six corps sont employés : \footnotesize, \small, \normalsize, \large, \Large, \Huge.
%
% 2. AUCUN RESSORT ÉLASTIQUE ENTRE DEUX BLOCS VOISINS. Pas un seul \vfill : la page était
%    auparavant construite sur des ressorts, et elle se recomposait entièrement dès qu'un champ
%    changeait de longueur. Tous les espaces sont fixes, en millimètres, et la page ne bouge donc
%    plus quand un nom est déposé ou retiré.
%
% 3. AUCUNE IMAGE tant que le logotype n'est pas déposé. `\logoEcole` compose un cadre de la
%    hauteur déclarée en attendant, et le contrôle d'absence d'image reste vert.
%
% Aucune valeur n'est écrite ici : chaque champ vient d'un marqueur, et tout champ dont le marqueur
% peut être vide passe par `\siRenseigne` — l'intitulé disparaît avec sa valeur.

\begin{titlepage}
\setlength{\parskip}{0pt}
\setstretch{1.0}
\centering

% La mention de version de travail, en haut à droite : elle signale l'état du document sans
% occuper le centre de la page.
\hfill\siBrouillon{\fbox{\small\mentionVersionDeTravail}}\par
\vspace*{6mm}

\logoEcole{2.2cm}

\vspace{8mm}

\siRenseigne{\marqueurEtablissementFormation}{%
  {\large\scshape \marqueurEtablissementFormation\par}\vspace{2mm}%
}
\siRenseigne{\marqueurFiliere}{%
  {\normalsize\itshape\color{griscartouche} \marqueurFiliere\par}%
}

\vspace{5mm}
{\color{griscartouche}\rule{\linewidth}{0.4pt}}

\vspace{30mm}

{\large\scshape\color{griscartouche} Rapport de stage d'application\par}

\vspace{6mm}

{\Huge\bfseries Chaîne de données\\[2mm] et tableau de bord\par}

\vspace{5mm}

{\Large pour un établissement hospitalier\par}

\vspace{7mm}
{\color{griscartouche}\rule{0.35\linewidth}{0.4pt}}

\vspace{32mm}

% Le bloc des noms. UNE SEULE COLONNE D'INTITULÉS, alignée à droite, et une colonne de valeurs qui
% commence toujours au même endroit : les trois intitulés sont des chaînes fixes, si bien que la
% largeur de leur colonne ne dépend d'aucun marqueur. Déposer un nom ne déplace donc rien.
\begin{tabular}{@{}r@{\hspace{6mm}}l@{}}
  \siRenseigne{\marqueurAuteur}{{\scshape\color{griscartouche} Réalisé par} & \marqueurAuteur \\[3mm]}
  \siRenseigne{\marqueurEncadrantProfessionnel}{{\scshape\color{griscartouche} Encadrant de stage} & \marqueurEncadrantProfessionnel \\[3mm]}
  {\scshape\color{griscartouche} Organisme d'accueil} & \marqueurOrganismeAccueil \\
\end{tabular}

\vspace{26mm}

\siRenseigne{\marqueurDateSoutenance}{%
  {\small\color{griscartouche} Soutenu le \marqueurDateSoutenance\par}\vspace{2mm}%
}
\siRenseigne{\marqueurAnneeUniversitaire}{%
  {\small\color{griscartouche} Année universitaire \marqueurAnneeUniversitaire\par}%
}

\end{titlepage}
