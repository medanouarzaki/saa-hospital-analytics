% Ce chapitre repose sur :
%   DOC: (aucun)
%   OBS: REL-RDV.C02, REL-RDV.R08
%   HYP: (aucun)
%   CHI: agregats, dim-patient-fiches-plusieurs-versions, dim-patient-lignes, dimensions, faits, identifiants-distincts
%   CHI: modeles-intermediate, modeles-marts, modeles-total, partitions-par-table, plancher-rejets, quarantaine-lignes-total
%   CHI: quarantaine-patients-lignes, quarantaine-rendez-vous-lignes, seuil-quarantaine, source-colonnes-texte, source-lignes-total, source-tables
%   CHI: taches-graphe
%   SER: (aucune)
%   SECTIONS: 7
\chapter{Architecture de la chaîne de données}\label{chap:architecture}

\section{Vue d'ensemble}

La chaîne va d'un dépôt de fichiers partitionnés à un tableau de bord, en cinq étapes dont chacune
a une raison d'exister distincte des autres.

\begin{center}
\begin{tabular}{@{}c@{}}
\fbox{\begin{minipage}{0.90\linewidth}\centering\small
\fbox{\begin{minipage}{0.94\linewidth}\centering \textbf{Zone d'atterrissage} \\
\footnotesize fichiers partitionnés par table et par date d'extraction\end{minipage}}\\[2pt]
$\downarrow$ \footnotesize chargement : contrôles d'entrée, quarantaine, transaction par partition\\[2pt]
\fbox{\begin{minipage}{0.44\linewidth}\centering \texttt{source} \\
\footnotesize copie fidèle, tout en texte\end{minipage}}%
\hfill
\fbox{\begin{minipage}{0.44\linewidth}\centering \texttt{quarantaine} \\
\footnotesize lignes rejetées, avec leur motif\end{minipage}}\\[2pt]
$\downarrow$ \footnotesize transformation : typage, normalisation\\[2pt]
\fbox{\begin{minipage}{0.94\linewidth}\centering \texttt{intermediate} \\
\footnotesize une vue par table source, colonnes typées\end{minipage}}\\[2pt]
$\downarrow$ \footnotesize modélisation dimensionnelle\\[2pt]
\fbox{\begin{minipage}{0.94\linewidth}\centering \texttt{marts} \\
\footnotesize dimensions, faits, agrégats\end{minipage}}\\[2pt]
$\downarrow$ \footnotesize rapprochement d'identités \quad$\rightarrow$\quad \texttt{linkage}\\[2pt]
$\downarrow$ \footnotesize échange de noms en une transaction\\[2pt]
\fbox{\begin{minipage}{0.94\linewidth}\centering \texttt{instantane} \\
\footnotesize tables figées que le tableau de bord lit\end{minipage}}
\end{minipage}}
\end{tabular}
\end{center}

\begin{center}
\footnotesize Architecture de la chaîne. Chaque cadre porte le nom que le code donne au schéma ou au
répertoire correspondant.
\end{center}

\medskip

Le principe qui gouverne cet enchaînement est qu'\textbf{aucune couche ne modifie la précédente}. La
couche source est une copie fidèle des fichiers, défauts compris ; la couche intermédiaire type et
normalise sans juger ; la couche analytique modélise. Une correction appliquée à l'entrée aurait
effacé ce qu'il fallait mesurer, et une correction appliquée en sortie aurait rendu la chaîne
irreproductible.

Ce que cette architecture dit des données, une seconde figure le dit des outils : chaque brique de
la pile, son rôle, et la couche à laquelle elle appartient.

\begin{center}
\begin{tikzpicture}[x=1mm, y=1mm]

\node[coucheUML, anchor=east] at (-4,0)   {restitution};
\node[coucheUML, anchor=east] at (-4,-20) {ordonnancement};
\node[coucheUML, anchor=east] at (-4,-40) {identités};
\node[coucheUML, anchor=east] at (-4,-60) {modélisation};
\node[coucheUML, anchor=east] at (-4,-80) {persistance};

\node[briqueUML, text width=52mm] (res) at (26,0)   {Bibliothèque d'application web analytique\\[1pt]\emph{lire, filtrer, afficher}};
\node[briqueUML, text width=52mm] (ord) at (26,-20) {Orchestrateur de graphes de tâches\\[1pt]\emph{déclencher dans l'ordre}};
\node[briqueUML, text width=52mm] (rap) at (26,-40) {Bibliothèque de rapprochement probabiliste\\[1pt]\emph{estimer, apparier, regrouper}};
\node[briqueUML, text width=52mm] (mod) at (26,-60) {Outil de transformation déclaratif\\[1pt]\emph{typer, modéliser, tester}};
\node[briqueUML, text width=52mm] (per) at (26,-80) {Serveur de base de données relationnelle\\[1pt]\emph{stocker, servir, transiger}};

\node[briqueOptUML, text width=44mm] (sch) at (96,-60) {Schéma en étoile\\[1pt]\emph{faits, dimensions, agrégats}};
\node[briqueOptUML, text width=44mm] (mem) at (96,-40) {Moteur en mémoire\\[1pt]\emph{calculer hors du serveur}};

\draw[fluxUML] (per) -- (mod);
\draw[fluxUML] (mod) -- (rap);
\draw[fluxUML] (rap) -- (ord);
\draw[fluxUML] (ord) -- (res);
\draw[fluxUML] (sch.west) -- (mod.east);
\draw[fluxUML] (mem.west) -- (rap.east);
\end{tikzpicture}
\end{center}

\begin{center}
\footnotesize La pile technique, couche par couche. Chaque brique porte la désignation que le
chapitre~\ref{chap:cadrage} lui donne — une désignation fonctionnelle et non un nom de produit — et
son rôle en trois verbes. Les flèches pleines montent d'une couche à la suivante ; les deux briques
en pointillé ne sont pas des couches mais des choix internes à celle qu'elles rejoignent. Aucun
logotype d'éditeur ne figure ici : ce sont des marques déposées. La figure ne montre ni les versions,
ni les dépendances, ni le conteneur qui porte chaque service.
\end{center}

\medskip

La chaîne compte \chiffre{modeles-total} modèles de transformation —
\chiffre{modeles-intermediate} intermédiaires et \chiffre{modeles-marts} analytiques — et
\chiffre{taches-graphe} tâches d'\emph{orchestration}, c'est-à-dire de tâches qu'un ordonnanceur
déclenche dans un ordre imposé par leurs dépendances.

\textbf{Une sixième étape mérite d'être expliquée, parce qu'elle n'est pas une couche de
transformation.} Le tableau de bord ne lit pas la couche analytique directement : il lit un schéma
d'\emph{instantané} — une copie figée et datée de l'état construit —, fait de tables et non de
vues. Le motif est mesuré — une reconstruction de la couche
analytique fait disparaître ses vues pendant environ trois dixièmes de seconde, et un lecteur
concurrent y rencontre une erreur d'objet inexistant, non une attente. Le rafraîchissement construit
donc des tables neuves sous des noms provisoires, puis échange tous les noms dans \emph{une seule}
transaction. Ce que l'instantané porte est une règle et non une liste : une copie de chaque vue
analytique, de chaque table de rapprochement, plus deux tables de service. La construction vise à
ce qu'un lecteur ne voie jamais un instantané mi-neuf mi-ancien ; aucun contrôle du dépôt ne
l'établit.

\section{Zone d'atterrissage et couche source}

La zone d'atterrissage est un dépôt de fichiers, partitionné par table et par date d'extraction :
\chiffre{partitions-par-table} partitions par table sur la période, soit une par jour. Cette
partition n'existe pas dans le système observé — c'est une construction de la chaîne, et elle a deux
effets : elle rend le chargement rejouable partition par partition, et elle donne à chaque ligne une
date d'extraction qui devient une colonne de la couche source.

Chaque partition est écrite \textbf{en deux formats}, un fichier de valeurs séparées et un fichier
colonnaire, et un contrôle vérifie que les deux portent le même contenu, colonne par colonne. Le
chargeur lit le format tabulaire ; le format colonnaire est un livrable d'échange, non une entrée
de la chaîne.

\textbf{La couche source ne porte aucune contrainte, et c'est délibéré.} Ses
\chiffre{source-tables} tables comptent \chiffre{source-colonnes-texte} colonnes, \emph{toutes de
type texte} — pas de clé primaire, pas de clé étrangère, pas de contrainte de domaine, pas de type
date ni numérique. Une contrainte y ferait échouer le chargement sur une ligne défectueuse, alors
que ces lignes sont précisément la matière du travail de qualité. Le typage est reporté à la couche
suivante, où il devient une transformation observable. Sur la période,
la couche source porte \chiffre{source-lignes-total} lignes.

\textbf{Le chargement d'une partition est une transaction unique.} Il commence par supprimer, pour
la date concernée, le contenu des deux schémas — la couche source et la quarantaine —, puis insère
les lignes acceptées d'un côté et les lignes rejetées de l'autre. Le retrait préalable est ce qui
rend l'opération rejouable : c'est le mécanisme d'idempotence de la section~5.7, et il vit ici, à
l'entrée, plutôt que dans la couche dimensionnelle.

Un contrôle porte sur la partition elle-même : chaque ligne d'un fichier doit déclarer la date
d'extraction du répertoire qui la contient, et une ligne qui en déclarerait une autre part en
quarantaine.

\section{Le choix du serveur de base de données}

Un moteur embarqué aurait suffi à produire les mêmes tables. Le serveur relationnel est retenu pour
des propriétés que le moteur embarqué ne met pas en jeu, et que cette chaîne exerce réellement : le
modèle client-serveur — plusieurs services attaquent la même base par le réseau, avec des
identifiants distincts —, les transactions concurrentes — le chargement écrit pendant que le tableau
de bord lit —, et le catalogue interrogeable, dont ce rapport se sert pour compter ses propres
colonnes.

Le motif n'est donc pas la performance : un moteur embarqué serait plus rapide sur ce volume.

Le serveur est conteneurisé, ce qui donne une base identique sur la machine de développement et chez
l'exécuteur d'intégration continue.

Le rapprochement d'identités fait exception et s'exécute sur un moteur en mémoire : il écrit de
nombreuses tables intermédiaires, et les tenir hors du serveur évite d'y laisser des résidus.

\section{Couche intermédiaire et quarantaine}

La couche intermédiaire compte \chiffre{modeles-intermediate} modèles, un par table source, et fait
trois choses : elle type, elle normalise, elle ne rejette rien.

\textbf{Le typage s'appuie sur des formats mesurés, non supposés.} Les dates de la couche source
sont au format mois-jour-année et les horodatages y ajoutent un indicateur de méridien — c'est une
observation de l'écran, relevée au poste sur la date de création d'un
rendez-vous\releve{REL-RDV.C02} comme sur sa date de tenue\releve{REL-RDV.R08}, et non une
convention posée. La
conversion est écrite une fois, dans une macro, et appliquée partout : sans cela, un jour et un mois
s'échangeraient silencieusement onze fois sur douze sans qu'aucune valeur ne devienne invalide. La
valeur manquante est de même la chaîne vide et jamais l'absence de valeur, ce qui a été mesuré sur
la base avant d'être écrit dans les macros.

\textbf{Une ligne refusée est mise en \emph{quarantaine} — un schéma parallèle où elle est conservée
avec le motif de son rejet — et jamais supprimée.} Le chargeur applique ses
contrôles d'entrée, écrit les lignes acceptées dans la couche source et les lignes rejetées dans un
schéma parallèle, avec le motif du rejet et la partition dont elles proviennent. Sur la période,
\chiffre{quarantaine-lignes-total} lignes y sont — \chiffre{quarantaine-patients-lignes} fiches
patient et \chiffre{quarantaine-rendez-vous-lignes} rendez-vous. Supprimer ces lignes aurait rendu impossible toute mesure de la qualité d'entrée.

\textbf{Un seuil bloque le chargement, et il est doublé d'un plancher.} Au-delà de
\chiffre{seuil-quarantaine} de lignes rejetées dans une partition, le fichier est bloqué plutôt que
chargé partiellement : un taux de rejet aussi élevé signale plus probablement un défaut d'extraction
en amont qu'une accumulation de défauts de saisie. Le plancher de \chiffre{plancher-rejets} rejets
évite l'effet de bord d'une petite partition, où un rejet unique dépasse mécaniquement tout seuil
exprimé en proportion. \textbf{Les deux conditions doivent être réunies pour bloquer.}

Un second contrôle porte sur le cumul de la journée, toutes tables confondues, et compare ce cumul
au même seuil. Sans lui, \chiffre{source-tables} fichiers restant chacun sous le seuil passeraient
alors que la journée entière serait mauvaise.

\textbf{Trois faits de la couche source ont été mesurés avant d'être écrits dans les conversions.}
La valeur manquante est toujours la chaîne vide et jamais l'absence de valeur. Les dates et
horodatages se convertissent tous, sur les cinquante-deux colonnes typées, sans une seule valeur non
convertible. Le domaine booléen est exactement composé de deux valeurs et de la chaîne vide.

\section{Modélisation dimensionnelle}

La couche analytique compte \chiffre{modeles-marts} modèles, et le schéma qu'ils produisent porte
\chiffre{dimensions} dimensions, \chiffre{faits} faits et \chiffre{agregats} agrégats. Les deux
décomptes ne sont pas le même et ne s'additionnent pas : l'un des agrégats est une vue
\emph{posée hors de l'outil de transformation}, par un fichier de définition de schéma appliqué en
préalable d'initialisation, et il n'est donc pas l'un des modèles. Son fichier de propriétés le dit
explicitement et donne le motif de ce maintien à part ; l'outil de transformation le signale
d'ailleurs de lui-même à l'analyse, ne trouvant aucun modèle qui corresponde à cet objet
documenté.

Ces objets sont organisés en \emph{schéma en étoile} — une forme où chaque table d'événements est
entourée des tables qui la décrivent, et où toute interrogation part du centre pour aller vers les
branches.

\begin{center}
\begin{tabular}{@{}c@{}}
\fbox{\begin{minipage}{0.92\linewidth}\centering\small
\fbox{\begin{minipage}{0.28\linewidth}\centering \texttt{dim\_date}\end{minipage}}%
\hfill
\fbox{\begin{minipage}{0.28\linewidth}\centering \texttt{dim\_patient} \\ \footnotesize historisée\end{minipage}}%
\hfill
\fbox{\begin{minipage}{0.28\linewidth}\centering \texttt{dim\_service}\end{minipage}}\\[3pt]
% CHAQUE FLÈCHE EST CENTRÉE DANS UNE BOÎTE DE LA LARGEUR DE LA DIMENSION QU'ELLE RELIE. Avec un
% `\hfill` entre elles, les deux flèches obliques étaient rejetées aux bords du cadre et
% s'arrêtaient dans le vide, à côté des boîtes qu'elles devaient joindre. Les trois dimensions
% occupent chacune 0,28 de la largeur ; les trois boîtes de flèche prennent la même largeur.
\makebox[0.28\linewidth]{$\searrow$}\hfill\makebox[0.28\linewidth]{$\downarrow$}\hfill\makebox[0.28\linewidth]{$\swarrow$}\\[3pt]
\fbox{\begin{minipage}{0.94\linewidth}\centering \textbf{Les six faits} \\[2pt]
\footnotesize
\texttt{fct\_sejour} — un séjour \quad
\texttt{fct\_passage} — un passage, les trois types \\
\texttt{fct\_passage\_urgence} — un passage aux urgences \quad
\texttt{fct\_rendez\_vous} — un rendez-vous \\
\texttt{fct\_facturation} — une facture \quad
\texttt{fct\_encaissement} — un encaissement
\end{minipage}}\\[3pt]
\makebox[0.28\linewidth]{$\nearrow$}\hfill\makebox[0.28\linewidth]{$\uparrow$}\hfill\makebox[0.28\linewidth]{$\nwarrow$}\\[3pt]
\fbox{\begin{minipage}{0.28\linewidth}\centering \texttt{dim\_activite}\end{minipage}}%
\hfill
\fbox{\begin{minipage}{0.28\linewidth}\centering \texttt{dim\_agent}\end{minipage}}%
\hfill
\fbox{\begin{minipage}{0.28\linewidth}\centering \texttt{dim\_organisme}\end{minipage}}
\end{minipage}}
\end{tabular}
\end{center}

\begin{center}
\footnotesize Le schéma en étoile : six dimensions entourant six faits. Les
\chiffre{agregats} agrégats, non représentés, se calculent depuis les faits.
\end{center}

\medskip

\textbf{Le grain de chaque fait est déclaré, et deux d'entre eux méritent d'être dits.} Le fait des
séjours porte un séjour par ligne, et il n'est pas une reprise ligne à ligne : il \emph{agrège} la
couche des mouvements, qui porte une ligne par mouvement — une admission, au plus une mutation, au
plus une sortie. Le fait des passages est une table pivot couvrant les trois types d'épisode, et
c'est à elle que se raccordent les factures et les prises en charge ; le fait des passages aux
urgences en est un satellite, dont l'espace de clés est un sous-ensemble strict.

Les autres grains sont directs : une facture, un encaissement — le grain est l'encaissement et non
la facture, une facture pouvant recevoir plusieurs règlements —, un rendez-vous.

\textbf{Quatre dimensions sur six ne portent que leur clé naturelle}, sans aucun attribut, et sans
clé de substitution. Ce n'est pas un oubli : aucune source ne nomme les libellés correspondants, et
écrire un libellé plausible en face d'un code reviendrait à afficher une hypothèse avec l'apparence
d'une donnée relevée. Les deux dimensions restantes font exception : le calendrier est construit et
non extrait, et la dimension patient porte l'historique, objet de la section suivante.

\textbf{Les \chiffre{agregats} agrégats ne sont pas des faits.} Un fait porte un événement, un
agrégat porte un décompte déjà calculé sur une maille d'affichage — l'activité par jour,
l'absentéisme par activité, le recouvrement par type de débiteur. Ils existent parce que le tableau
de bord doit rendre une page en un temps borné, et qu'un calcul sur les faits à chaque affichage ne
le permettrait pas. Un chiffre repris d'un agrégat n'est donc pas recalculé depuis les faits, et le
registre des indicateurs porte, pour chaque entrée affichée, ce dont sa valeur est recalculée.

Deux agrégats ne pourraient pas l'être de toute façon : il n'existe ni fait des créances ni fait des
relances, et la matière n'est disponible qu'en couche intermédiaire.

\section{La dimension patient historisée}

Les cinq autres dimensions écrasent leurs valeurs ; la dimension patient conserve l'historique.
Le motif est qu'une fiche patient change — une adresse est corrigée, une couverture est mise à jour
— et qu'un fait daté doit se rattacher à la version en vigueur au moment de l'événement, non à la
dernière connue.

Elle porte \chiffre{dim-patient-lignes} lignes, qui sont des versions, pour
\chiffre{identifiants-distincts} identifiants distincts ;
\chiffre{dim-patient-fiches-plusieurs-versions} identifiants en portent plus d'une.

\textbf{La clé de fusion est le couple identifiant et début de validité}, et non l'identifiant seul.
Les intervalles sont semi-ouverts : le début est inclus, la fin est exclue, et la version courante
porte une fin absente. Deux versions consécutives se touchent donc exactement, sans se chevaucher ni
laisser de trou.

Trois contrôles distincts la vérifient à chaque construction : la continuité — pour toute version
non courante, la fin égale exactement le début de la suivante —, le non-chevauchement, et le fait
qu'un identifiant porte une version courante et une seule. Les trois sont nécessaires : la
continuité seule laisserait passer deux versions courantes, et le non-chevauchement seul laisserait
passer un trou.

\textbf{Ce que l'historisation permet en aval.} Un fait daté ne se rattache pas à l'identifiant du
patient mais au couple identifiant et début de validité : il pointe vers la version en vigueur au
moment de l'événement. Une adresse corrigée en mai ne réécrit donc pas la provenance géographique
d'un passage de janvier. C'est la propriété que les cinq autres dimensions n'offrent pas, et c'est
la raison pour laquelle celle-ci seule est historisée : le coût — une ligne par version plutôt
qu'une par entité — ne se justifie que là où la valeur change et où l'antériorité compte.

Le décompte le dit : \chiffre{dim-patient-lignes} lignes pour
\chiffre{identifiants-distincts} identifiants, soit un surcoût de stockage d'environ un huitième,
pour une propriété que rien d'autre ne rendrait.

\section{Orchestration, idempotence et rattrapage}

Le graphe compte \chiffre{taches-graphe} tâches, en chaîne, et son ordre porte une contrainte de
modélisation : \textbf{les dimensions se construisent avant les faits}, un fait se rattachant à une
version de dimension.

Le diagramme de séquence ci-dessous les porte dans leur ordre, entre les participants réels de la
chaîne.

\begin{center}
\begin{tikzpicture}[x=1mm, y=1mm]

% --- les six participants, et leurs lignes de vie ------------------------------
\foreach \i/\x/\nom in {1/0/{La zone\\d'atterrissage}, 2/23/{Le chargeur}, 3/46/{La base},
                        4/69/{L'outil de\\transformation}, 5/92/{Le rapprochement},
                        6/115/{L'instantané}}{
  \node[partUML] (p\i) at (\x,0) {\nom};
  \draw[vieUML] (p\i.south) -- ++(0,-126);
}

\def\msg#1#2#3#4{\draw[msgUML] (#1.south|-0,#3) -- (#2.south|-0,#3) node[etiqUML,midway] {#4};}

\msg{p1}{p2}{-8}{1. vérifier la disponibilité de l'extraction}
\msg{p2}{p3}{-19}{2. charger la journée}

% Le contrôle de qualité s'exécute sur la base elle-même : message réflexif.
\draw[msgUML] (p3.south|-0,-28) -- ++(7,0) -- ++(0,-5) -- ++(-7,0);
\node[etiqUML, anchor=west] at (54,-30) {3. contrôle de qualité — taux de rejet cumulé};

\msg{p3}{p4}{-42}{4. couche intermédiaire}
\msg{p4}{p3}{-50}{5. \textbf{les dimensions}}
\msg{p3}{p4}{-58}{6. \textbf{puis les faits}}
\msg{p4}{p3}{-66}{7. tests de la couche}
\msg{p3}{p5}{-76}{8. prédiction des paires}
\msg{p5}{p3}{-84}{9. regroupement et évaluation}
\msg{p3}{p4}{-94}{10. agrégats}
\msg{p3}{p6}{-104}{11. rafraîchir l'instantané}

% Le rafraîchissement échange les noms des tables : réflexif, et en une seule transaction.
\draw[msgUML] (p6.south|-0,-110) -- ++(7,0) -- ++(0,-5) -- ++(-7,0);
\node[etiqUML, anchor=east] at (113,-112) {\textbf{échange de tous les noms en une seule transaction}};

% Le douzième message sort du diagramme : il n'a pas de destinataire parmi les participants.
\draw[msgUML] (p3.south|-0,-122) -- ++(-40,0) node[etiqUML,midway] {12. exporter — fichiers de livraison} -- ++(-12,0);
\fill[accentgarde] (p3.south|-0,-122) ++(-52,0) circle (0.6mm);
\end{tikzpicture}
\end{center}

\begin{center}
\footnotesize Le traitement quotidien entre ses six participants. Les douze messages sont les
douze tâches du graphe, dans leur ordre, et chacune porte au code l'identifiant que voici :
\texttt{verifier\_disponibilite\_extraction}, \texttt{charger\_journee}, \texttt{controle\_qualite},
\texttt{dbt\_intermediaire}, \texttt{dbt\_dimensions}, \texttt{dbt\_faits}, \texttt{dbt\_tests},
\texttt{rapprochement\_prediction}, \texttt{rapprochement\_regroupement\_evaluation},
\texttt{dbt\_agregats}, \texttt{rafraichir\_instantane}, \texttt{exporter}. et deux traits de ce dessin ne se lisent pas sur la liste
qui précède. \textbf{Les dimensions se construisent avant les faits} — messages~5 et~6 —, un fait
se rattachant à une version de dimension. Et le rafraîchissement de l'instantané n'est pas une
reconstruction : les tables neuves sont bâties à côté, puis \textbf{tous les noms sont échangés en
une seule transaction}, si bien qu'un lecteur ne voit jamais un état mixte. Le douzième message
sort du diagramme parce que son destinataire n'est pas un participant : ce sont des fichiers.
La figure ne montre ni les reprises sur échec, ni le verrou consultatif qui interdit deux
rafraîchissements simultanés, ni le rattrapage d'une journée manquante.
\end{center}

\medskip

\textbf{Une opération est \emph{idempotente} lorsque la refaire ne change rien : la faire deux fois
donne le même résultat que la faire une fois.} C'est le chargement qui porte cette propriété ici,
et non la couche dimensionnelle.

Prenons le cas concret. Le fichier du 12 mars a été chargé, puis on s'aperçoit qu'il était
incomplet ; on le corrige et on le recharge. Le chargement commence par supprimer, dans une même
transaction, tout ce qui portait la date du 12 mars — dans la couche source comme en quarantaine —
avant d'insérer. La journée du 12 mars est donc remplacée, jamais dédoublée.

\textbf{Le \emph{rattrapage} en découle : c'est le fait de rejouer une journée manquante sans
toucher aux autres.} Si les journées du 12, 13 et 14 mars manquent, les charger les rattrape, et
l'ordre dans lequel on le fait ne change pas l'état final. Deux contrôles l'établissent, l'un en
chargeant deux fois la même partition, l'autre en chargeant les mêmes partitions dans deux ordres
différents.

\textbf{Deux tâches manquent volontairement à ce graphe.} La production du schéma et son
application à la base sont des opérations destructrices — les fichiers de définition suppriment
leurs tables avant de les recréer — et les placer dans un graphe quotidien reviendrait à vider la
base chaque nuit. Elles s'exécutent à la main, au moment où le modèle change.

\textbf{La construction des modèles s'exécute sur un seul fil.} Les modèles se lisent les uns les
autres pendant leur construction, et un parallélisme y produit des lectures d'objets en cours de
remplacement — le même phénomène qui a motivé le schéma d'instantané.

\textbf{Le graphe se termine par deux tâches de sortie} : le rafraîchissement de l'instantané, qui
rend l'état construit visible au tableau de bord en une transaction, et l'export tabulaire, qui
écrit un fichier par table. Ce livrable est régénéré à chaque exécution, et son répertoire n'est pas
suivi par le gestionnaire de versions.
