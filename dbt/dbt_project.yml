name: "saa_hospital_analytics"
version: "1.0.0"

profile: "saa_hospital_analytics"

model-paths: ["models"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  saa_hospital_analytics:
    +materialized: view
    intermediate:
      +schema: intermediate
    marts:
      +schema: marts

seeds:
  saa_hospital_analytics:
    +schema: referentiels

vars:
  dim_date_debut: "2023-01-01"
  dim_date_fin: "2026-12-31"
